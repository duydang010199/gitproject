let maxtrix = [[numbers], [numbers], [numbers], [numbers]];
let size = 5;
let min = 10,
  max = 50;
let numbers = new Array(size);
for (i = 1; i < array.length; i++) {
  numbers[i] = Math.floor(Math.random() * (max - min + 1) + min);
}
document.write(numbers[i]);
